<% if @products.any? %>
	<% @products.each do %>
	<h2><%= product.name %></h2>
	<h3><%= product.price %></h3>
	<p><%= product.description %></p>
	<% end %>
	<% else %>
		<p>No products yet!</p>
<% end %>	
<a href="#">Add a product</a>